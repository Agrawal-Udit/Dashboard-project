---
phase: 02-state-layer
plan: 04
type: execute
wave: 3
depends_on:
  - 02-03
files_modified: []
autonomous: false
requirements:
  - ROLE-02

must_haves:
  truths:
    - "Selected role survives a full browser page reload (F5 / Ctrl+R)"
    - "darkMode preference survives a full browser page reload"
    - "localStorage key 'finance-dashboard-store' contains { role, darkMode } after store mutation"
    - "Transactions are NOT written to localStorage (only role and darkMode persisted)"
  artifacts:
    - path: "src/store/store.ts"
      provides: "persist middleware with partialize — already created in Plan 03"
  key_links:
    - from: "browser localStorage"
      to: "src/store/store.ts persist rehydration"
      via: "Zustand persist onRehydrateStorage lifecycle"
      pattern: "finance-dashboard-store"
---

<objective>
Manual browser verification that ROLE-02 is satisfied: the selected role and darkMode persist correctly across a full page refresh.

Purpose: Zustand persist hydration requires a real browser reload cycle with real localStorage. This cannot be automated in Vitest — it requires the human to observe the store state in DevTools before and after pressing F5.
Output: Confirmed observable behavior documented in SUMMARY.md. Phase 2 complete.
</objective>

<execution_context>
@C:/Users/Hp/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Hp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-state-layer/02-03-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Verify: Role and darkMode persist across browser page reload (ROLE-02)</name>
  <action>
No automated implementation — this checkpoint verifies observable browser behavior that cannot be tested in Vitest. All prior plans have already implemented the persist middleware. This task confirms it functions end-to-end in a real browser environment.
  </action>
  <what-built>
    Complete Phase 2 state layer: Zustand store with txnSlice (CRUD), uiSlice (role + darkMode), persist middleware (role + darkMode persisted to localStorage key 'finance-dashboard-store'), and selector hooks (useTransactions + useSummaryTotals).

    All 5 automated test files are GREEN. This checkpoint verifies the one behavior that requires a real browser reload cycle: ROLE-02 — selected role persists across page refresh.
  </what-built>
  <how-to-verify>
    1. Start the dev server: `npm run dev` in E:/Udit
    2. Open http://localhost:5173 in a browser
    3. Open browser DevTools (F12) → Console tab
    4. In the Console, manually write a persist state to localStorage:
       ```javascript
       localStorage.setItem('finance-dashboard-store', JSON.stringify({
         state: { role: 'Admin', darkMode: true },
         version: 1
       }))
       ```
    5. Press F5 to reload the page
    6. In DevTools → Application → Local Storage → http://localhost:5173
       Verify the key `finance-dashboard-store` exists and still contains `"role":"Admin"` and `"darkMode":true`
    7. In the Console, run:
       ```javascript
       JSON.parse(localStorage.getItem('finance-dashboard-store'))
       ```
       Expected output: `{ state: { role: 'Admin', darkMode: true }, version: 1 }`
    8. Verify that `transactions` does NOT appear in the localStorage value (partialize excludes it)

    PASS conditions:
    - localStorage key 'finance-dashboard-store' exists after page load
    - role and darkMode values survive the reload
    - transactions array is absent from localStorage
    - No console errors about Zustand or localStorage

    FAIL conditions:
    - localStorage key missing or empty after reload
    - role resets to 'Viewer' after reload (persist not working)
    - Console shows "Maximum update depth exceeded" (useShallow missing in a hook)
    - transactions array appears in localStorage (partialize misconfigured)
  </how-to-verify>
  <verify>
    <automated>MANUAL — Role persistence requires browser reload cycle. Run `npm run dev` and follow how-to-verify steps above.</automated>
  </verify>
  <done>localStorage key 'finance-dashboard-store' contains role and darkMode after page reload; transactions absent from localStorage; no console errors</done>
  <resume-signal>Type "approved" if all PASS conditions met, or describe any failures observed</resume-signal>
</task>

</tasks>

<verification>
Human confirms:
1. `finance-dashboard-store` key in localStorage contains `role` and `darkMode`
2. Values survive a full page reload (F5)
3. `transactions` is absent from localStorage value
4. No React maximum-update-depth error in console
</verification>

<success_criteria>
- ROLE-02 confirmed: role persists across page refresh
- darkMode persistence confirmed as side-effect
- No regressions in browser console
- Phase 2 all requirements complete: FOUND-04, FOUND-05, ROLE-02
</success_criteria>

<output>
After completion, create `.planning/phases/02-state-layer/02-04-SUMMARY.md`
</output>
