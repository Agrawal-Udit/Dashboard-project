---
phase: 06-polish
plan: 03
type: execute
wave: 2
depends_on:
  - 06-02
files_modified:
  - src/utils/motionConfig.ts
  - src/App.tsx
  - src/components/ui/Card.tsx
  - src/components/ui/Modal.tsx
  - src/components/dashboard/BalanceTrendChart.tsx
  - src/components/dashboard/SpendingPieChart.tsx
  - src/components/dashboard/KpiCard.tsx
  - src/pages/DashboardPage.tsx
  - src/components/dashboard/__tests__/BalanceTrendChart.test.tsx
  - src/components/dashboard/__tests__/SpendingPieChart.test.tsx
  - src/components/ui/__tests__/primitives.test.tsx
autonomous: true
requirements:
  - UX-05
  - UX-06

must_haves:
  truths:
    - "Route transitions, cards, modal, and charts have subtle motion in normal mode"
    - "Reduced-motion mode disables or minimizes movement-heavy animation on all target surfaces"
    - "Modal close-state behavior remains test-compatible and accessible"
    - "KPI cards display meaningful empty-state messaging when no transaction data exists"
  artifacts:
    - path: "src/utils/motionConfig.ts"
      provides: "Central surface-specific motion presets with reduced-motion fallback"
      exports: ["MotionSurface", "getSurfaceMotion"]
    - path: "src/App.tsx"
      provides: "Location-keyed route transition wrapper"
    - path: "src/components/ui/Modal.tsx"
      provides: "Animated overlay/panel while preserving accessibility and close behavior"
    - path: "src/components/dashboard/KpiCard.tsx"
      provides: "Optional emptyMessage display branch"
    - path: "src/pages/DashboardPage.tsx"
      provides: "KPI empty-state message wiring based on transaction emptiness"
  key_links:
    - from: "src/App.tsx"
      to: "src/utils/motionConfig.ts"
      via: "getSurfaceMotion('route', shouldReduceMotion)"
      pattern: "getSurfaceMotion\('route'"
    - from: "src/components/ui/Card.tsx"
      to: "src/utils/motionConfig.ts"
      via: "getSurfaceMotion('card', shouldReduceMotion)"
      pattern: "getSurfaceMotion\('card'"
    - from: "src/components/ui/Modal.tsx"
      to: "src/utils/motionConfig.ts"
      via: "getSurfaceMotion('modal', shouldReduceMotion)"
      pattern: "getSurfaceMotion\('modal'"
    - from: "src/pages/DashboardPage.tsx"
      to: "src/components/dashboard/KpiCard.tsx"
      via: "emptyMessage prop"
      pattern: "emptyMessage"
---

<objective>
Implement reduced-motion-aware animation polish and complete card-level empty-state communication.

Purpose: satisfy UX-05 and UX-06 with accessibility-safe motion and explicit no-data UX on KPI cards.
Output: central motion presets + integrated motion wiring + KPI card empty-message support.
</objective>

<execution_context>
@C:/Users/Hp/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Hp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-polish/06-VALIDATION.md

<interfaces>
Motion API:
```typescript
import { motion, AnimatePresence, useReducedMotion } from 'motion/react'
```

Existing App routing:

```typescript
<BrowserRouter>
  <Routes>...</Routes>
</BrowserRouter>
```

Constraint for route transitions:

- `useLocation()` must be called inside a component rendered under `BrowserRouter`.
- If needed, split `App` into a router shell and `AnimatedRoutes` child.

Modal constraint:

- Closed modal content should remain unmounted for compatibility with existing primitive tests and accessibility expectations.
  </interfaces>
  </context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement centralized motion system and wire route/card/modal/chart surfaces</name>
  <files>
    src/utils/motionConfig.ts,
    src/App.tsx,
    src/components/ui/Card.tsx,
    src/components/ui/Modal.tsx,
    src/components/dashboard/BalanceTrendChart.tsx,
    src/components/dashboard/SpendingPieChart.tsx
  </files>
  <action>
    Create `motionConfig.ts`:
    - export `MotionSurface = 'route' | 'card' | 'modal' | 'chart'`
    - export `getSurfaceMotion(surface, reduced)`
    - reduced mode: duration `0`, no translate/scale movement-heavy transitions

    Route transitions:
    - introduce an inner routed component (e.g., `AnimatedAppRoutes`) under `BrowserRouter`
    - use `useLocation()` there
    - use `AnimatePresence` + location-keyed `motion` wrapper with route preset

    Card/Modal/Chart integrations:
    - convert card wrapper to motion-enabled root
    - modal backdrop/panel animate in normal mode; preserve unmounted closed-state
    - in charts, apply motion wrapper + set Recharts `isAnimationActive={!shouldReduceMotion}`

    Avoid dual-control transition conflicts:
    - do not combine motion transforms with Tailwind transform-transition classes on the same node.

  </action>
  <verify>
    <automated>npx vitest run src/utils/__tests__/motionConfig.test.ts src/components/ui/__tests__/primitives.test.tsx src/components/dashboard/__tests__/BalanceTrendChart.test.tsx src/components/dashboard/__tests__/SpendingPieChart.test.tsx</automated>
  </verify>
  <done>Motion config and wiring-level tests pass green; modal and chart behavior remain stable under reduced-motion and normal modes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add KPI card empty-state messaging and wire from DashboardPage</name>
  <files>
    src/components/dashboard/KpiCard.tsx,
    src/pages/DashboardPage.tsx
  </files>
  <action>
    Extend `KpiCard` props with `emptyMessage?: string`.

    Rendering contract:
    - default branch (no `emptyMessage`): existing currency-focused rendering
    - empty branch (`emptyMessage` present): render contextual helper copy as primary body content while retaining label/icon context

    Update `DashboardPage`:
    - compute `isEmpty = transactions.length === 0`
    - pass card-specific messages when empty:
      - Balance: "No transaction data yet"
      - Income: "No income data yet"
      - Expenses: "No expense data yet"

  </action>
  <verify>
    <automated>npx vitest run src/components/dashboard/__tests__/KpiCard.test.tsx src/components/dashboard/__tests__/KpiCard.emptyState.test.tsx</automated>
  </verify>
  <done>KPI default tests and new empty-state tests pass green; no regressions in dashboard rendering.
</done>
</task>

</tasks>

<verification>
After both tasks:

npx vitest run
npx tsc --noEmit
</verification>

<success_criteria>

- Motion is present on route/card/modal/chart in normal mode
- Reduced-motion mode is consistently honored on those same surfaces
- Modal closed-state remains unmounted and accessible behavior is preserved
- KPI cards show meaningful no-data messages when transactions are empty
  </success_criteria>

<output>
After completion, create `.planning/phases/06-polish/06-03-SUMMARY.md`
</output>
