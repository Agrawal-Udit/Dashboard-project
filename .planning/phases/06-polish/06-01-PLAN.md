---
phase: 06-polish
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - src/utils/__tests__/exportUtils.test.ts
  - src/components/transactions/__tests__/TransactionExportActions.test.tsx
  - src/utils/__tests__/motionConfig.test.ts
  - src/components/dashboard/__tests__/KpiCard.emptyState.test.tsx
autonomous: true
requirements:
  - UX-03
  - UX-04
  - UX-05
  - UX-06

must_haves:
  truths:
    - "Wave 0 RED tests exist for export security/fidelity and motion behavior"
    - "Export tests enforce visible-scope-only contract"
    - "Motion tests enforce reduced-motion behavior for all target surfaces"
    - "KPI card empty-state contract is encoded in tests before implementation"
  artifacts:
    - path: "src/utils/__tests__/exportUtils.test.ts"
      provides: "RED tests for CSV/JSON serialization contracts and formula-injection mitigation"
    - path: "src/components/transactions/__tests__/TransactionExportActions.test.tsx"
      provides: "RED tests for export action wiring to visibleTransactions"
    - path: "src/utils/__tests__/motionConfig.test.ts"
      provides: "RED tests for reduced-motion presets"
    - path: "src/components/dashboard/__tests__/KpiCard.emptyState.test.tsx"
      provides: "RED tests for KPI card no-data messaging"
  key_links:
    - from: "src/utils/__tests__/exportUtils.test.ts"
      to: "src/utils/exportUtils.ts"
      via: "import { sanitizeCsvCell, transactionsToCsv, transactionsToJson } from '../exportUtils'"
      pattern: "sanitizeCsvCell|transactionsToCsv|transactionsToJson"
    - from: "src/components/transactions/__tests__/TransactionExportActions.test.tsx"
      to: "src/components/transactions/TransactionExportActions.tsx"
      via: "import { TransactionExportActions } from '../TransactionExportActions'"
      pattern: "TransactionExportActions"
    - from: "src/components/transactions/__tests__/TransactionExportActions.test.tsx"
      to: "src/utils/exportUtils.ts"
      via: "vi.mock('../../../utils/exportUtils')"
      pattern: "../../../utils/exportUtils"
    - from: "src/utils/__tests__/motionConfig.test.ts"
      to: "src/utils/motionConfig.ts"
      via: "import { getSurfaceMotion } from '../motionConfig'"
      pattern: "getSurfaceMotion"
---

<objective>
Create all Wave 0 RED test stubs for Phase 6 before any implementation work.

Purpose: lock contracts for export security/fidelity, reduced-motion behavior, and KPI card empty-state semantics.
Output: four RED test files with explicit acceptance contracts for Plans 02 and 03.
</objective>

<execution_context>
@C:/Users/Hp/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Hp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-polish/06-VALIDATION.md

<interfaces>
From src/types/index.ts:
```typescript
export interface Transaction {
  id: string
  date: string
  amount: number
  category: Category
  type: TransactionType
  description: string
}
```

Planned contracts introduced in Phase 6:

```typescript
// src/utils/exportUtils.ts
export function sanitizeCsvCell(value: string): string;
export function transactionsToCsv(rows: Transaction[]): string;
export function transactionsToJson(rows: Transaction[]): string;
export function downloadTextFile(
  filename: string,
  content: string,
  mimeType: string,
): void;

// src/utils/motionConfig.ts
export type MotionSurface = "route" | "card" | "modal" | "chart";
export function getSurfaceMotion(
  surface: MotionSurface,
  reduced: boolean,
): {
  initial: Record<string, number>;
  animate: Record<string, number>;
  exit?: Record<string, number>;
  transition: { duration: number; ease: string };
};

// src/components/transactions/TransactionExportActions.tsx
interface TransactionExportActionsProps {
  visibleTransactions: Transaction[];
}
```

</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create RED tests for export utilities and export action wiring</name>
  <files>
    src/utils/__tests__/exportUtils.test.ts,
    src/components/transactions/__tests__/TransactionExportActions.test.tsx
  </files>
  <action>
    Create `src/utils/__tests__/exportUtils.test.ts` with RED tests covering:
    - deterministic CSV headers and stable row ordering
    - formula-injection mitigation for dangerous-leading values
    - quote escaping rules
    - JSON ordering + numeric type fidelity
    - immutability of input arrays

    Create `src/components/transactions/__tests__/TransactionExportActions.test.tsx` with RED tests covering:
    - Export CSV uses `visibleTransactions`
    - Export JSON uses `visibleTransactions`
    - correct MIME + extension per action

    Mock export utility module using a correct relative path from the test file:
    - `vi.mock('../../../utils/exportUtils')`

  </action>
  <verify>
    <automated>npx vitest run src/utils/__tests__/exportUtils.test.ts src/components/transactions/__tests__/TransactionExportActions.test.tsx</automated>
  </verify>
  <done>Both files exist and are RED with contract-driven failures (module-not-found or expectation failures are expected at this stage).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create RED tests for motion contracts and KPI card empty state</name>
  <files>
    src/utils/__tests__/motionConfig.test.ts,
    src/components/dashboard/__tests__/KpiCard.emptyState.test.tsx
  </files>
  <action>
    Create `src/utils/__tests__/motionConfig.test.ts` with RED tests covering:
    - reduced=true sets duration 0 for route/card/modal/chart
    - reduced=true removes translate/scale movement-heavy transforms
    - reduced=false retains non-zero transitions for modal/route

    Create `src/components/dashboard/__tests__/KpiCard.emptyState.test.tsx` with RED tests covering:
    - optional `emptyMessage` rendering
    - empty-state branch suppresses normal value-emphasis semantics

  </action>
  <verify>
    <automated>npx vitest run src/utils/__tests__/motionConfig.test.ts src/components/dashboard/__tests__/KpiCard.emptyState.test.tsx</automated>
  </verify>
  <done>Both files exist and fail RED as intended, clearly defining implementation targets for Plan 03.</done>
</task>

</tasks>

<verification>
Run full suite once after stubs are added:

npx vitest run

Expected:

- New Wave 0 files fail RED.
- Existing test files remain GREEN.
  </verification>

<success_criteria>

- Four Wave 0 test files are created at exact paths
- RED contracts explicitly cover UX-03/04/05/06 semantics
- Test failure output is intentional and implementation-guiding
  </success_criteria>

<output>
After completion, create `.planning/phases/06-polish/06-01-SUMMARY.md`
</output>
