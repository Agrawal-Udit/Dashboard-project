---
phase: 06-polish
plan: 02
type: execute
wave: 1
depends_on:
  - 06-01
files_modified:
  - src/utils/exportUtils.ts
  - src/components/transactions/TransactionExportActions.tsx
  - src/pages/TransactionsPage.tsx
autonomous: true
requirements:
  - UX-03
  - UX-04

must_haves:
  truths:
    - "CSV and JSON exports use only currently visible filtered/sorted transactions"
    - "CSV serialization mitigates spreadsheet formula injection"
    - "JSON serialization preserves current ordering and native field types"
    - "Export actions are available in transactions UI without breaking role-gated add/edit behavior"
  artifacts:
    - path: "src/utils/exportUtils.ts"
      provides: "Pure CSV/JSON serializers plus browser download helper"
      exports: ["sanitizeCsvCell", "transactionsToCsv", "transactionsToJson", "downloadTextFile"]
    - path: "src/components/transactions/TransactionExportActions.tsx"
      provides: "Export CSV/JSON actions bound to visibleTransactions prop"
      exports: ["TransactionExportActions"]
    - path: "src/pages/TransactionsPage.tsx"
      provides: "Visible-data export wiring in header action row"
  key_links:
    - from: "src/pages/TransactionsPage.tsx"
      to: "src/components/transactions/TransactionExportActions.tsx"
      via: "<TransactionExportActions visibleTransactions={transactions} />"
      pattern: "visibleTransactions=\{transactions\}"
    - from: "src/components/transactions/TransactionExportActions.tsx"
      to: "src/utils/exportUtils.ts"
      via: "imports transactionsToCsv/transactionsToJson/downloadTextFile"
      pattern: "transactionsToCsv|transactionsToJson|downloadTextFile"
---

<objective>
Implement secure export features for visible transaction data only.

Purpose: satisfy UX-03 and UX-04 with deterministic serialization, spreadsheet safety, and clean page integration.
Output: export utility module + export action component + transactions page wiring.
</objective>

<execution_context>
@C:/Users/Hp/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Hp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-polish/06-VALIDATION.md
@.planning/phases/06-polish/06-RESEARCH.md

<interfaces>
From src/hooks/useTransactions.ts:
```typescript
export function useTransactions(filters?: TransactionFilters): Transaction[]
// returns visible filtered + sorted rows
```

Planned utility contract:

```typescript
export function sanitizeCsvCell(value: string): string;
export function transactionsToCsv(rows: Transaction[]): string;
export function transactionsToJson(rows: Transaction[]): string;
export function downloadTextFile(
  filename: string,
  content: string,
  mimeType: string,
): void;
```

Planned component contract:

```typescript
interface TransactionExportActionsProps {
  visibleTransactions: Transaction[];
}
```

</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement exportUtils with security + fidelity guarantees</name>
  <files>src/utils/exportUtils.ts</files>
  <action>
    Create a pure utility module with no React dependencies:

    1) `sanitizeCsvCell(value: string)`
    - converts to string
    - prefixes apostrophe `'` when value starts with `=`, `+`, `-`, `@`, tab, or carriage return
    - escapes double quotes (`"` => `""`)

    2) `transactionsToCsv(rows: Transaction[])`
    - fixed column order: `date,description,category,type,amount`
    - preserves input ordering exactly
    - outputs header even for empty input
    - keeps amount as numeric string (not localized currency)

    3) `transactionsToJson(rows: Transaction[])`
    - returns `JSON.stringify(rows, null, 2)`

    4) `downloadTextFile(filename, content, mimeType)`
    - Blob + object URL + temporary anchor click + cleanup (`URL.revokeObjectURL`)

  </action>
  <verify>
    <automated>npx vitest run src/utils/__tests__/exportUtils.test.ts</automated>
  </verify>
  <done>Export utility tests pass green, including formula-mitigation, quote escaping, order fidelity, and immutability checks.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add export action component and wire it to visible transactions</name>
  <files>
    src/components/transactions/TransactionExportActions.tsx,
    src/pages/TransactionsPage.tsx
  </files>
  <action>
    Create `TransactionExportActions` with two buttons:
    - Export CSV
    - Export JSON

    Behavior:
    - inputs are from `visibleTransactions` prop only
    - CSV uses `transactionsToCsv` + `downloadTextFile` (`text/csv;charset=utf-8`)
    - JSON uses `transactionsToJson` + `downloadTextFile` (`application/json;charset=utf-8`)
    - filename includes date suffix for traceability

    Update `TransactionsPage`:
    - render export actions in the header action group
    - pass `visibleTransactions={transactions}` from existing filtered/sorted hook output
    - keep Add Transaction button admin-only via existing RoleGate

  </action>
  <verify>
    <automated>npx vitest run src/components/transactions/__tests__/TransactionExportActions.test.tsx</automated>
  </verify>
  <done>Export action tests pass green and page compiles with visible-array binding.</done>
</task>

</tasks>

<verification>
After both tasks:

npx vitest run src/utils/**tests**/exportUtils.test.ts src/components/transactions/**tests**/TransactionExportActions.test.tsx
npx tsc --noEmit
</verification>

<success_criteria>

- CSV/JSON export features are implemented and tested
- CSV output is spreadsheet-safe against leading-formula injection
- Exported dataset matches exactly what user currently sees in transactions view
- TypeScript compile remains clean
  </success_criteria>

<output>
After completion, create `.planning/phases/06-polish/06-02-SUMMARY.md`
</output>
