---
phase: 06-polish
plan: 04
type: execute
wave: 3
depends_on:
  - 06-03
files_modified:
  - src/components/transactions/TransactionTable.tsx
  - src/components/transactions/TransactionFilters.tsx
  - src/components/transactions/TransactionExportActions.tsx
  - src/pages/TransactionsPage.tsx
  - src/components/layout/AppLayout.tsx
  - src/components/transactions/__tests__/TransactionTable.test.tsx
autonomous: false
requirements:
  - UX-07
  - UX-03
  - UX-04
  - UX-05
  - UX-06

must_haves:
  truths:
    - "Transactions view is usable at 375px, 768px, and 1280px+ without control overlap"
    - "No horizontal scrolling is required for core transaction interactions at 375px"
    - "Header action row (title + export + add) remains accessible and non-overlapping on narrow screens"
    - "Responsive hardening does not regress export, motion, or empty-state behavior"
  artifacts:
    - path: "src/components/transactions/TransactionTable.tsx"
      provides: "mobile-safe transaction presentation with action parity"
    - path: "src/pages/TransactionsPage.tsx"
      provides: "responsive-safe header action-row composition"
    - path: "src/components/transactions/TransactionFilters.tsx"
      provides: "wrapped/stacked filter controls at narrow widths"
    - path: "src/components/layout/AppLayout.tsx"
      provides: "safe container behavior for narrow viewport content"
  key_links:
    - from: "src/pages/TransactionsPage.tsx"
      to: "src/components/transactions/TransactionExportActions.tsx"
      via: "header action-row composition with responsive wrapping"
      pattern: "TransactionExportActions"
    - from: "src/components/transactions/TransactionTable.tsx"
      to: "src/components/auth/RoleGate.tsx"
      via: "mobile and desktop render paths both preserve edit role-gating"
      pattern: "RoleGate"
---

<objective>
Finalize responsive hardening and complete Phase 6 blocking QA checks.

Purpose: satisfy UX-07 and confirm no regressions across UX-03..UX-06 after responsive adjustments.
Output: responsive-safe transactions/layout surfaces + approved manual final checkpoint.
</objective>

<execution_context>
@C:/Users/Hp/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Hp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-polish/06-VALIDATION.md
@.planning/phases/06-polish/06-02-SUMMARY.md
@.planning/phases/06-polish/06-03-SUMMARY.md

<interfaces>
From `TransactionTable`:
```typescript
interface TransactionTableProps {
  transactions: Transaction[]
  sortBy: 'date' | 'amount' | 'category'
  sortOrder: 'asc' | 'desc'
  onSort: (column: 'date' | 'amount' | 'category') => void
  onEdit: (transaction: Transaction) => void
}
```

Breakpoint targets:

- mobile: 375px
- tablet: 768px
- desktop: 1280px+

Testing constraint:

- jsdom does not apply responsive CSS breakpoints.
- if both mobile and desktop variants render simultaneously in test DOM, update tests or use conditional rendering strategy to avoid duplicate-query failures.
  </interfaces>
  </context>

<tasks>

<task type="auto">
  <name>Task 1: Implement responsive hardening for transactions and action-row layout</name>
  <files>
    src/components/transactions/TransactionTable.tsx,
    src/components/transactions/TransactionFilters.tsx,
    src/components/transactions/TransactionExportActions.tsx,
    src/pages/TransactionsPage.tsx,
    src/components/layout/AppLayout.tsx,
    src/components/transactions/__tests__/TransactionTable.test.tsx
  </files>
  <action>
    Transactions header/action row (`TransactionsPage` + export actions):
    - ensure title + export buttons + add button wrap/stack safely at 375px
    - preserve full functionality for Viewer and Admin roles

    Filters (`TransactionFilters`):
    - remove rigid width constraints that force overflow
    - allow type buttons, category select, and search to wrap/stack naturally on small screens

    Table/list (`TransactionTable`):
    - retain sortable desktop table on md+
    - provide mobile-safe rendering path for <md with readable transaction cards/rows and edit action parity
    - preserve role-gated edit controls in both render paths

    Layout shell (`AppLayout`):
    - ensure narrow viewport content can shrink/wrap correctly (e.g., `min-w-0` discipline)
    - avoid masking layout bugs via broad `overflow-x-hidden` hacks

    Tests:
    - adjust/expand `TransactionTable.test.tsx` if responsive dual-path rendering introduces duplicate-query ambiguity in jsdom

  </action>
  <verify>
    <automated>npx vitest run src/components/transactions/__tests__/TransactionTable.test.tsx</automated>
  </verify>
  <done>Transaction responsive behavior is updated without breaking table semantics or role-gated actions.
</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Final blocking QA checkpoint</name>
  <action>Run the manual validation checklist below and only proceed when all checks are approved.</action>
  <what-built>
    Final responsive hardening + full Phase 6 regression checkpoint.
  </what-built>
  <how-to-verify>
    Start app: `npm run dev`

    A) UX-03 CSV (visible + safe)
    1. Filter/sort/search transactions.
    2. Export CSV.
    3. Verify exported rows match visible rows only.
    4. Verify dangerous-leading value (e.g., `=2+2`) is safely prefixed in CSV.

    B) UX-04 JSON (visible + type/order fidelity)
    1. Keep filtered/sorted view.
    2. Export JSON.
    3. Verify ordering matches visible table/list ordering.
    4. Verify `amount` values are numeric (not quoted strings).

    C) UX-05 Motion and reduced-motion
    1. Validate normal-mode route/card/modal/chart motion.
    2. Emulate `prefers-reduced-motion: reduce`.
    3. Confirm movement-heavy animations are disabled/reduced.

    D) UX-06 Empty states
    1. Apply no-match search filter.
    2. Confirm meaningful list empty-state message.
    3. Confirm dashboard card/chart empty-state messaging remains coherent.

    E) UX-07 Responsive targets
    1. Verify at widths: 375, 768, 1280.
    2. Confirm no overlapping action controls.
    3. Confirm no horizontal scroll required for primary user flows.
    4. Confirm filters, table/cards, export/add actions remain usable.

  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or report failing step + viewport + page.</resume-signal>
</task>

</tasks>

<verification>
Before manual checkpoint:

npx vitest run
npx tsc --noEmit
npm run build
</verification>

<success_criteria>

- Responsive behavior is verified at 375/768/1280 with no overlap and no required horizontal scrolling for core flows
- Export, motion, and empty-state features remain correct after responsive changes
- Final manual checkpoint approved
  </success_criteria>

<output>
After completion, create `.planning/phases/06-polish/06-04-SUMMARY.md`
</output>
