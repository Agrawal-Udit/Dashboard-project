---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - vite.config.ts
  - vitest.config.ts
  - tsconfig.json
  - tsconfig.app.json
  - index.html
  - src/main.tsx
  - src/App.tsx
  - src/setupTests.ts
  - src/utils/__tests__/financeUtils.test.ts
  - src/utils/__tests__/dateUtils.test.ts
  - src/data/__tests__/mockData.test.ts
autonomous: true
requirements:
  - FOUND-01
  - FOUND-02
  - FOUND-03

must_haves:
  truths:
    - "npx vitest run exits non-zero (RED) because test files reference functions that do not exist yet — scaffolding is verified as the starting state"
    - "npx tsc --noEmit exits zero — scaffold TypeScript compiles cleanly before any source files are added"
    - "npm run build exits zero — Vite can produce a production bundle from the empty scaffold"
    - "All three test files exist with failing assertions covering FOUND-01, FOUND-02, and FOUND-03 behaviors"
  artifacts:
    - path: "package.json"
      provides: "Vite 8 + React 19 + TypeScript scaffold with date-fns, vitest, and @vitest/coverage-v8 installed"
    - path: "vitest.config.ts"
      provides: "Vitest 3.x configured with environment:node, coverage, and setupFiles pointing to src/setupTests.ts"
    - path: "src/utils/__tests__/financeUtils.test.ts"
      provides: "Failing tests for calcTotals, groupByCategory, formatChartData (FOUND-03)"
      contains: "describe('calcTotals'), describe('groupByCategory'), describe('formatChartData')"
    - path: "src/utils/__tests__/dateUtils.test.ts"
      provides: "Failing tests for formatCurrency, formatDate (FOUND-03)"
      contains: "describe('formatCurrency'), describe('formatDate')"
    - path: "src/data/__tests__/mockData.test.ts"
      provides: "Failing tests for seed data shape and coverage (FOUND-02)"
      contains: "describe('MOCK_TRANSACTIONS')"
  key_links:
    - from: "vitest.config.ts"
      to: "src/setupTests.ts"
      via: "setupFiles array"
      pattern: "setupFiles.*setupTests"
    - from: "src/utils/__tests__/financeUtils.test.ts"
      to: "src/utils/financeUtils.ts"
      via: "import statement (file does not exist yet — intentionally RED)"
      pattern: "from.*financeUtils"
---

<objective>
Bootstrap the Vite + React 19 + TypeScript scaffold into E:/Udit and write all test stubs that will drive implementation in Plans 02 and 03. This plan ends in a RED state by design: tests exist and assert correct behavior, but the source files they import do not exist yet.

Purpose: Establish the test harness before any implementation so every subsequent code file is driven by a failing test. This is the Wave 0 contract — no implementation plan runs until this plan is complete.

Output: Runnable Vite scaffold, installed dependencies, Vitest config, and three test files with failing assertions covering all FOUND-01, FOUND-02, and FOUND-03 behaviors.
</objective>

<execution_context>
@C:/Users/Hp/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Hp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Vite project and install Phase 1 dependencies</name>
  <files>package.json, vite.config.ts, tsconfig.json, tsconfig.app.json, index.html, src/main.tsx, src/App.tsx</files>
  <action>
Run the following commands in sequence from E:/Udit:

1. Verify Node version is 20.19+ or 22.12+ — Vite 8 drops Node 18:
   ```
   node --version
   ```
   If the output is v18.x, STOP and report the Node version issue. The scaffold cannot proceed on Node 18.

2. Scaffold Vite into the current directory (dot = E:/Udit, which already has .git and .planning):
   ```
   npm create vite@latest . -- --template react-ts
   ```
   When prompted about existing files, choose to keep existing files (do not overwrite .git or .planning/).

3. Install core runtime dependency for Phase 1 (date-fns only — no Recharts, Zustand, or React UI libs yet):
   ```
   npm install date-fns
   ```

4. Install dev dependencies for testing:
   ```
   npm install -D vitest @vitest/coverage-v8 @testing-library/jest-dom jsdom
   ```

5. Delete the generated placeholder files that will be replaced in later phases:
   - Delete src/App.css (will not be used — Tailwind replaces it in Phase 3)
   - Delete src/assets/ directory
   - Replace the contents of src/App.tsx with a minimal placeholder:
     ```typescript
     export default function App() {
       return <div>Finance Dashboard — Phase 1 Foundation</div>
     }
     ```

Do NOT run npm install for: recharts, zustand, tailwindcss, @tailwindcss/vite, motion, react-hook-form, zod, clsx, lucide-react, react-papaparse. Those belong to phases 2–6.
  </action>
  <verify>
    <automated>cd E:/Udit && node --version && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>node --version prints v20.x or v22.x, npm run build exits 0 with no errors, dist/ directory exists.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Configure Vitest and write all failing test stubs</name>
  <files>vitest.config.ts, src/setupTests.ts, src/utils/__tests__/financeUtils.test.ts, src/utils/__tests__/dateUtils.test.ts, src/data/__tests__/mockData.test.ts</files>
  <behavior>
    All tests MUST be RED (failing) when this task ends. They import from source files that do not exist yet. This is the intended TDD starting state.

    financeUtils.test.ts covers:
    - calcTotals([]) returns {balance:0, income:0, expenses:0}
    - calcTotals(MOCK_TRANSACTIONS).balance === income - expenses
    - calcTotals only counts income transactions for income total
    - calcTotals only counts expense transactions for expenses total
    - groupByCategory returns only expense transactions (income entries excluded)
    - groupByCategory result has name, value, color keys on every item
    - groupByCategory([]) returns []
    - formatChartData([]) returns []
    - formatChartData output — every item has date, income, expenses, balance keys
    - formatChartData output — no item has NaN or undefined values
    - formatChartData output — items are sorted chronologically (earliest date first)

    dateUtils.test.ts covers:
    - formatCurrency(0) returns "$0.00"
    - formatCurrency(-150) returns "-$150.00"
    - formatCurrency(null) returns "$0.00" (no throw)
    - formatCurrency(undefined) returns "$0.00" (no throw)
    - formatCurrency(1234.56) returns "$1,234.56"
    - formatDate(null) returns "Invalid date" (no throw)
    - formatDate(undefined) returns "Invalid date" (no throw)
    - formatDate("not-a-date") returns "Invalid date" (no throw)
    - formatDate("2026-01-15") returns "Jan 15, 2026"
    - formatDate("2026-01-15", "MMM yyyy") returns "Jan 2026"

    mockData.test.ts covers:
    - MOCK_TRANSACTIONS.length is between 25 and 30 (inclusive)
    - Every transaction has id, date, amount, category, type, description fields
    - Every transaction has amount > 0 (no negative amounts)
    - At least 3 distinct months exist across all transaction dates
    - Both 'income' and 'expense' types are present
    - At least 3 income transactions exist (salary, freelance, or investment)
    - All 11 category ids appear at least once (housing, food, transport, utilities, entertainment, healthcare, education, salary, freelance, investment, other)
  </behavior>
  <action>
Step 1 — Create vitest.config.ts at E:/Udit/vitest.config.ts:
```typescript
import { defineConfig } from 'vitest/config'

export default defineConfig({
  test: {
    environment: 'node',
    globals: true,
    setupFiles: ['./src/setupTests.ts'],
    coverage: {
      provider: 'v8',
      reporter: ['text', 'json', 'html'],
      include: ['src/utils/**', 'src/data/**', 'src/types/**'],
    },
  },
})
```

Use environment: 'node' (not jsdom). Phase 1 tests are pure functions with no DOM dependency. jsdom adds startup overhead and is not needed until Phase 3 component tests.

Step 2 — Create src/setupTests.ts:
```typescript
import '@testing-library/jest-dom'
```

Step 3 — Create src/utils/__tests__/financeUtils.test.ts. Import paths must point to the files that will exist after Plan 03 (even though they don't exist yet):
```typescript
import { describe, it, expect } from 'vitest'
import { calcTotals, groupByCategory, formatChartData } from '../financeUtils'
import { MOCK_TRANSACTIONS } from '../../data/mockData'
```
Write the full test cases listed in the behavior block above using Vitest's describe/it/expect API.

Step 4 — Create src/utils/__tests__/dateUtils.test.ts:
```typescript
import { describe, it, expect } from 'vitest'
import { formatCurrency, formatDate } from '../dateUtils'
```
Write all formatCurrency and formatDate test cases from the behavior block.

Step 5 — Create src/data/__tests__/mockData.test.ts:
```typescript
import { describe, it, expect } from 'vitest'
import { MOCK_TRANSACTIONS } from '../mockData'
```
Write all MOCK_TRANSACTIONS assertions from the behavior block. For the "all 11 categories present" test, collect unique category ids and check each required id with expect(ids).toContain('housing') etc.

Step 6 — Update tsconfig.app.json to include vitest types. Add "vitest/globals" to compilerOptions.types array if it exists, or create the types array:
```json
{
  "compilerOptions": {
    "types": ["vitest/globals"]
  }
}
```

Do NOT create src/utils/financeUtils.ts, src/utils/dateUtils.ts, or src/data/mockData.ts — those are Plan 02 and 03 deliverables. The tests must fail due to missing modules.
  </action>
  <verify>
    <automated>cd E:/Udit && npx vitest run 2>&1 | tail -20</automated>
  </verify>
  <done>npx vitest run fails with "Cannot find module '../financeUtils'" (or similar module-not-found error) on all three test files. The failure is due to missing source modules, NOT due to test syntax errors. Zero test files have syntax or parse errors — only import failures. npx tsc --noEmit still exits 0 (tsconfig allows missing imports in test files under allowJs or skipLibCheck).</done>
</task>

</tasks>

<verification>
Run these checks after both tasks complete:

1. `cd E:/Udit && node --version` — must print v20.x or v22.x
2. `cd E:/Udit && npm run build` — must exit 0
3. `cd E:/Udit && npx vitest run 2>&1` — must show test failures from missing module imports (not syntax errors)
4. Confirm all three test files exist: src/utils/__tests__/financeUtils.test.ts, src/utils/__tests__/dateUtils.test.ts, src/data/__tests__/mockData.test.ts
5. Confirm vitest.config.ts exists with environment: 'node'
</verification>

<success_criteria>
- Vite 8 scaffold is in E:/Udit with React 19 + TypeScript; npm run build exits 0
- date-fns, vitest, @vitest/coverage-v8, @testing-library/jest-dom installed
- vitest.config.ts configured with node environment and coverage provider
- Three test files exist covering all 15 test cases from VALIDATION.md
- npx vitest run is RED — fails on missing module imports (not syntax errors)
- No implementation source files created yet — this plan ends in RED state intentionally
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md` with:
- Scaffold command used and Node version confirmed
- Dependencies installed (runtime and dev)
- Test files created and their behavior coverage
- Confirmed RED state: which modules are missing
- Any deviations from plan (e.g., Vite scaffold prompts, file conflicts)
</output>
